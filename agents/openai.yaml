interface:
  display_name: "Teamwork"
  short_description: "Plan, review, execute, and verify complex implementation work"
  default_prompt: "Use $teamwork to break this request into a reviewed plan, execute it with Codex/Copilot routing, and run verification before completion."
