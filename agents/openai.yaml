interface:
  display_name: "Teamwork"
  short_description: "Plan, review, execute, verify, and final-review complex implementation work"
  default_prompt: "Use $teamwork to break this request into a reviewed plan, execute it with Codex/Copilot routing, run verification, then run final Codex review before completion."
